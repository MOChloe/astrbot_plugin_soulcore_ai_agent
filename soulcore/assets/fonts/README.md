# 随包中文字体

SoulCore 只随包分发一套中文字体：

- `SoulCoreSansSC-Regular.ttf`：由 Noto Sans SC 可变字体固定为 400 字重，并按 SIL Open Font License 1.1 的要求重命名。它统一供 PDF、社交截图和视觉模型探针使用，避免依赖宿主机字体。

该静态实例属于 Noto Sans SC 的修改版本，继续按 SIL Open Font License 1.1 分发；完整许可及上游版权声明见 `OFL-NotoSansSC.txt`。
